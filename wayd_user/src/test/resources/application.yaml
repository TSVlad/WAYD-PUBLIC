spring:
  profiles:
    active: test
  application:
    name: wayd-user


server:
  port: 8081
  servlet:
    context-path: /user-service-api


eureka:
  client:
    enabled: false

keycloak:
  enabled: false

wayd:
  kafka:
    address: localhost:9092
    groupId: user
    producer:
      id: user-producer
  confirmation-code:
    duration: 15
    length: 6

